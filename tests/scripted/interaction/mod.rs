//! Interaction audit: the durable interaction plane's runtime half.
//!
//! Proved through the real Agent Loop and conversation runtime. The store
//! half of the same contract lives in the external `durable` integration
//! target.

mod audit;
