{
  "$defs": {
    "AgentStatusExtensionDocument": {
      "additionalProperties": false,
      "description": "The authored Agent Status extension.\n\n`enabled` composes the extension in or out of the runtime as a whole;\n`time` and `background` remain the two bounded status contributors, with\nexactly the semantics they had before the migration.",
      "properties": {
        "background": {
          "$ref": "#/$defs/BackgroundStatusConfig",
          "default": {
            "enabled": true
          },
          "description": "The Background contributor configuration."
        },
        "enabled": {
          "default": true,
          "description": "Whether this composition includes the Agent Status extension at all.\n\nWith `false` the runtime composes no status engine: the Agent Loop\nemits no Agent Status and is otherwise a completely ordinary\n`ConversationRuntime`.",
          "type": "boolean"
        },
        "time": {
          "$ref": "#/$defs/TimeStatusConfig",
          "default": {
            "enabled": true
          },
          "description": "The Time contributor configuration."
        }
      },
      "type": "object"
    },
    "BackgroundStatusConfig": {
      "additionalProperties": false,
      "description": "Configuration of the bounded Background status contributor.",
      "properties": {
        "enabled": {
          "default": true,
          "description": "Whether Background participates in an available Agent Status opportunity.",
          "type": "boolean"
        }
      },
      "type": "object"
    },
    "ModelRef": {
      "description": "A fully qualified catalog model reference: `provider-id/model-id`.\n\nThe first `/` separates the provider from the model. The model ID itself\nmay contain additional `/` characters, as is common for Hugging Face\nidentities such as `Qwen/Qwen3`, but no model-ID segment may be empty.\n\nThis is the explicit model-identity domain of the runtime. Concatenated\nstrings never travel through the runtime in its place: a reference either\nresolves to exactly one catalog model or it fails.",
      "type": "string"
    },
    "NativeAgentExtensionsDocument": {
      "additionalProperties": false,
      "description": "The closed authored composition surface of native Agent Extensions.\n\nEvery member is a concrete named extension. The map-like spelling in\nJSONC (`\"extensions\": { \"agentStatus\": { ... } }`) is a *closed* record,\nnot an open registry: an unknown extension name is rejected by the\nsurrounding strict serde boundary exactly like any other unknown field.",
      "properties": {
        "agentStatus": {
          "$ref": "#/$defs/AgentStatusExtensionDocument",
          "default": {
            "background": {
              "enabled": true
            },
            "enabled": true,
            "time": {
              "enabled": true
            }
          },
          "description": "The Agent Status extension: optional provider-independent runtime\ncontext for an already-established model step."
        }
      },
      "type": "object"
    },
    "SubagentAgentsMdDocument": {
      "additionalProperties": false,
      "description": "The project-instruction policy of one named definition.",
      "properties": {
        "files": {
          "default": [],
          "description": "Explicit agent-owned project instruction files, in deterministic\nconfigured order. Relative paths resolve against the owning\nconfiguration document's directory at launch resolution.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "inherit": {
          "default": true,
          "description": "Whether the invoking generation's normal project instruction chain is\nprepended to the explicit files.",
          "type": "boolean"
        }
      },
      "type": "object"
    },
    "SubagentWorktreeDocument": {
      "additionalProperties": false,
      "description": "The JSONC representation of a named subagent's optional Git worktree\nisolation. An omitted or disabled value is the existing shared-workspace\nbehavior; there is no model-facing per-call override.\n\nIssue #188: when enabled, isolation runs the child from the committed\nparent `HEAD` snapshot. `require_clean_parent` defaults to `true`\n(strict): the parent source workspace must be clean, otherwise the child\nis rejected rather than silently dropping parent-local changes. Only an\nexplicit `\"requireCleanParent\": false` permits a dirty parent while the\nchild still receives exactly the committed snapshot.",
      "properties": {
        "enabled": {
          "default": false,
          "description": "Whether this named definition uses an isolated Git worktree.",
          "type": "boolean"
        },
        "requireCleanParent": {
          "default": true,
          "description": "Whether acquisition rejects a dirty parent workspace/index.\n\nThis is the one authoritative strictness switch, normalized at the\nconfiguration boundary: it defaults to `true` whenever isolation is\nenabled, and an explicit `false` is the intentional opt-out that runs\nfrom the captured committed `HEAD` while excluding dirty parent bytes.",
          "type": "boolean"
        }
      },
      "type": "object"
    },
    "TimeStatusConfig": {
      "additionalProperties": false,
      "description": "Configuration of the bounded Time status contributor.",
      "properties": {
        "enabled": {
          "default": true,
          "description": "Whether Time participates in an available Agent Status opportunity.",
          "type": "boolean"
        },
        "timezone": {
          "description": "The optional IANA timezone used only by Time presentation. Omitting it\nrenders UTC.",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "ToolSelectionDocument": {
      "additionalProperties": false,
      "description": "The authored map form of an exact, source-qualified capability selection.\n\nThis is the one selection vocabulary shared by every trusted authoring\nsurface: named-role frontmatter (`tools:`), a Workflow Agent node's\ninvocation override, and the model-facing `subagent` Tool's `override`.\nOrigins are named explicitly rather than collapsed into bare strings, so a\nBuiltin `read` and an MCP server's `read` are never interchangeable.\nWildcards are deliberately absent: a selection is an exact list. Managed\nPython tool packages are selected through `mcp` under their synthesized\nserver identity (`python:<folder>`, Issue #174).\n\nThe `python:` namespace is reserved for those synthesized identities:\n`mcpServers` configuration may never declare a server under it (rejected\nduring validation), so a selection naming `python:<folder>` always resolves\nto the managed package, never to a configured server.",
      "properties": {
        "builtin": {
          "default": [],
          "description": "Built-in/native capabilities, by canonical model-facing name.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "mcp": {
          "additionalProperties": {
            "items": {
              "type": "string"
            },
            "type": "array"
          },
          "default": {},
          "description": "MCP capabilities, keyed by server identity.",
          "type": "object"
        }
      },
      "type": "object"
    }
  },
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "description": "Strict canonical role Markdown frontmatter. The body supplies primary instructions.\n\nEverything here is *definition* state, and the definition is the child's\ncanonical **default** execution profile. One invocation may replace\n`tools`, `skills`, and `extensions` for exactly that child through the\nshared `SubagentInvocationOverride` (Issue #258), within an explicit\ndelegation ceiling. Every other field here — model, instructions,\n`timeoutMs`, `agentsMd`, `worktree` — has no per-call form at all.",
  "properties": {
    "agentsMd": {
      "$ref": "#/$defs/SubagentAgentsMdDocument",
      "default": {
        "files": [],
        "inherit": true
      },
      "description": "The project-instruction policy of this agent."
    },
    "description": {
      "description": "The bounded model-facing routing description.",
      "type": "string"
    },
    "extensions": {
      "$ref": "#/$defs/NativeAgentExtensionsDocument",
      "default": {
        "agentStatus": {
          "background": {
            "enabled": true
          },
          "enabled": true,
          "time": {
            "enabled": true
          }
        }
      },
      "description": "The closed **Native Agent Extension** composition of this named role\n(Issue #256).\n\nIndependently authored: a role never inherits the root Agent's\nextension set, and an omitted value means this role's own built-in\ndefaults, never the invoking runtime's configuration."
    },
    "model": {
      "anyOf": [
        {
          "$ref": "#/$defs/ModelRef"
        },
        {
          "type": "null"
        }
      ],
      "description": "The explicit model this agent runs on. Omit to inherit the invoking\nattempt's frozen effective model configuration."
    },
    "skills": {
      "default": [],
      "description": "The exact Skill allowlist over the admitted Skill catalog.",
      "items": {
        "type": "string"
      },
      "type": "array"
    },
    "timeoutMs": {
      "description": "The optional maximum wall-clock duration of the complete child\nlifecycle, in milliseconds. The model cannot override or extend it.",
      "format": "uint64",
      "minimum": 0,
      "type": [
        "integer",
        "null"
      ]
    },
    "tools": {
      "$ref": "#/$defs/ToolSelectionDocument",
      "default": {
        "builtin": [],
        "mcp": {}
      },
      "description": "The exact source-qualified capability selection."
    },
    "worktree": {
      "$ref": "#/$defs/SubagentWorktreeDocument",
      "default": {
        "enabled": false,
        "requireCleanParent": true
      },
      "description": "The bounded project-workspace policy of this agent."
    }
  },
  "required": [
    "description"
  ],
  "title": "SubagentDocument",
  "type": "object"
}
