# Minimal workspace

This ordinary project intentionally has no `rustx.toml` and no `.agents`
resources. Native Tools need no Python, uv, MCP, Subagents, or Workflows.
The endpoint and model declarations in the neighboring catalog are fixtures;
replace them with explicit declarations for your service before runtime use.
