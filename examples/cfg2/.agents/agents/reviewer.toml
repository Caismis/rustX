description = "Review Rust changes with explicitly selected capabilities."
instructions = "Inspect the requested change. Report concrete defects and evidence."
skills = ["rust-review"]

[tools]
builtin = ["read", "grep"]

[tools.sources]
github = "all"
"python:echo" = ["echo"]
# Deliberately undiscovered: suppress this selection; reviewer stays usable.
optional = ["annotate"]
