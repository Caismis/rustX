import { useState } from 'react';
import type { RuntimeLayer, SourceScope, SourceToolSelection, ModelLayer, AgentSkillSelection, AgentStatusExtensionDocument, AgentProjectInstructionsDocument } from '../../../../protocol/app-server/v15';
import { Button } from '../../presentation/primitives/Button';
import { ModelSelectionFields, StatusPluginFields } from './AgentEditor';
import { CheckboxList, Toggle, Names, nativeTools, Selection, TextField, UnitForm, type SaveSource } from './controls';

export type RootSection = 'root-model' | 'root-tools' | 'root-skills' | 'root-plugins' | 'root-agents';
export function RootEditor({ document, scope, revision, save, section, models, skillRoots }: { document: RuntimeLayer; scope: SourceScope; revision: string; save: SaveSource; section: RootSection; models: string[]; skillRoots: string[] }) {
  const agent = document.agent;
  const [source, setSource] = useState(''), [added, setAdded] = useState<string[]>([]), [family, setFamily] = useState('mcp');
  return <section aria-label="Root Agent">
    {section === 'root-model' && <><UnitForm<ModelLayer> title="Root model" initial={agent?.model ?? {}} revision={revision} save={save} mutation={authored => ({ kind: 'config', scope, mutation: { unit: 'root_model', authored } })}>{(value, change) => <ModelSelectionFields value={value} change={change} models={models} />}</UnitForm>
      <UnitForm<AgentProjectInstructionsDocument> title="Project guidance" initial={agent?.agents_md ?? {}} revision={revision} save={save} mutation={authored => ({ kind: 'config', scope, mutation: { unit: 'project_guidance', authored } })}>{(value, change) => <><label><input type="checkbox" checked={value.inherit ?? true} onChange={e => change({ ...value, inherit: e.target.checked })} />Include project guidance</label><Names label="Guidance files" value={value.files ?? []} change={files => change({ ...value, files })} /></>}</UnitForm></>}
    {section === 'root-tools' && <><UnitForm<string[]> title="Native Tools" initial={agent?.tools?.builtin ?? []} revision={revision} save={save} mutation={authored => ({ kind: 'config', scope, mutation: { unit: 'native_tools', authored } })}>{(value, change) => <CheckboxList label="Explicit whitelist" values={nativeTools} selected={value} change={change} />}</UnitForm>
      <p>Absent Root Native Tool selection grants no Tools. Source selections are atomic per MCP server or Python package.</p>
      {[...new Set([...Object.keys(agent?.tools?.sources ?? {}), ...added])].map(id => <UnitForm<SourceToolSelection> key={id} title={`Source ${id}`} initial={agent?.tools?.sources?.[id] ?? []} revision={revision} save={save} mutation={authored => ({ kind: 'config', scope, mutation: { unit: 'source_tools', id, authored } })}>{(value, change) => <Selection label={id} value={value} change={change} />}</UnitForm>)}
      <label>Source family<select value={family} onChange={e => setFamily(e.target.value)}><option value="mcp">MCP</option><option value="python">Managed Python</option></select></label><TextField label="Source identity" value={source} change={setSource} /><Button disabled={!source} onClick={() => { setAdded(current => [...current, family === 'python' ? `python:${source}` : source]); setSource(''); }}>Add source selection</Button>
    </>}
    {section === 'root-skills' && <><p>Skill selection controls prompt visibility, not filesystem access. Tools retain ordinary read behavior and progressive disclosure.</p><dl><dt>User Skill root</dt><dd>{skillRoots[0]}</dd><dt>Workspace Skill root</dt><dd>{skillRoots[1]}</dd></dl><UnitForm<AgentSkillSelection> title="Skill visibility" initial={agent?.skills ?? []} revision={revision} save={save} mutation={authored => ({ kind: 'config', scope, mutation: { unit: 'skills', authored } })}>{(value, change) => <Selection label="Visible Skills" value={value} change={change} />}</UnitForm></>}
    {section === 'root-plugins' && <><p>Closed native Plugins default off. Each Workspace Plugin replaces the same User Plugin as a whole. Current Todo and Goal state belongs to the Conversation.</p>{(['todo', 'goal'] as const).map(unit => <UnitForm<{ enabled?: boolean }> key={unit} title={`${unit} Plugin`} initial={agent?.plugins?.[unit] ?? {}} revision={revision} save={save} mutation={authored => ({ kind: 'config', scope, mutation: { unit, authored } })}>{(value, change) => <Toggle label={`Enable ${unit}`} checked={value.enabled ?? false} change={enabled => change({ enabled })} />}</UnitForm>)}<UnitForm<AgentStatusExtensionDocument> title="Agent Status Plugin" initial={agent?.plugins?.agent_status ?? {}} revision={revision} save={save} mutation={authored => ({ kind: 'config', scope, mutation: { unit: 'agent_status', authored } })}>{(value, change) => <StatusPluginFields value={value} change={change} />}</UnitForm></>}
    {section === 'root-agents' && <><p>Existence of a resource grants no Root authority. Named Agents own independent profiles; Root Tool and Plugin selections do not impose a ceiling on them.</p>{(['agents', 'workflows'] as const).map(unit => <UnitForm<string[]> key={unit} title={unit === 'agents' ? 'Agent allowlist' : 'Workflow allowlist'} initial={agent?.[unit] ?? []} revision={revision} save={save} mutation={authored => ({ kind: 'config', scope, mutation: { unit, authored } })}>{(value, change) => <Names label={unit} value={value} change={change} />}</UnitForm>)}</>}
  </section>;
}
